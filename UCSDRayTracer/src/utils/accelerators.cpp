//
//  accelerators.cpp
//  UCSDRayTracer
//
//  Created by Brendan Mulcahey on 7/5/23.
//

#include "accelerators.hpp"


/*
std::shared_ptr<primitive> MakeAccelerator(
    const std::string &name, std::vector<std::shared_ptr<primitive>> prims, const objParamMap &paramSet) {
std::shared_ptr<primitive> accel;
if (name == "bvh")
    accel = createBVHAccelerator(std::vector<std::shared_ptr<primitive>> prims, const objParamMap& paramSet);
        //later
else
    throw std::invalid_argument("error reading accelerator");

    
return accel;
}
*/
